fastapi
uvicorn[standard]
python-multipart
https://cdn.evilmadscientist.com/dl/ad/public/AxiDraw_API.zip
vpype

# Imported directly by app/svg_utils.py and app/main.py. It currently arrives
# anyway as a dependency of vpype, which is exactly why it belongs here: a
# fresh install works today by luck, and would break the moment vpype changed
# its own dependencies. Declared, not pinned — the version vpype settles on is
# the one this should use.
lxml

# Imported directly by app/svg_complexity.py, which uses a KD-tree over stroke
# endpoints to work out which vpype tolerance would bring an unplottable
# drawing back into range. Same reasoning as lxml above: both already arrive as
# dependencies of vpype, and relying on that is luck rather than a declaration.
numpy
scipy

# The "Save As" export feature (app/export.py). cairosvg rasterizes/prints the
# optimized SVG to PNG/PDF — pure Python, binds to the system libcairo, which is
# already present on the Pi. vpype-gcode adds vpype's `gwrite` command and the
# bundled `gcodemm` / `HPGL-R` profiles used for the G-code and HPGL exports.
cairosvg
vpype-gcode
